# Core packages
matplotlib>=3.6        # OK, modern version works
natsort>=7.1.1         # no need to pin exact version
numpy>=1.24            # modern Python 3.12 compatible
opencv-contrib-python>=4.8.0  # latest stable opencv-contrib

# Deep learning
torch>=2.1.0           # latest stable PyTorch
torchvision>=0.16.0    # matching torchvision for PyTorch 2.1
segmentation-models-pytorch>=0.3.0  # newer SMP version, works with modern torch

# Utilities
tqdm>=4.65.0           # modern tqdm

# Optional / custom packages
vcam>=1.0              # keep latest
